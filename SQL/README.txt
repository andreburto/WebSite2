WebSite2, SQL
----------------------------------------------------------------------

The SQL to create Procedures and Functions that run the blog engine:

 - CountPosts.sql
 - GetNewPostId.sql
 - NewPost.sql

The SQL to create the Posts table within the "andrew" database:

 - Posts_table.sql